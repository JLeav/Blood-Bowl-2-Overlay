package view.output;

import javax.swing.Icon;
import javax.swing.JLabel;

public class RaceField extends JLabel{

	public RaceField() {super();}
	public RaceField(String race) {super(race);}//For testing only, later won't be allowed to have a string
	public RaceField(Icon image) {super(image);}
	
}
